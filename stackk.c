#include "monty.h"

/**
 * print_top - This function prints the top element of the stack
 * @head: pointer to the stack head
 * @counter: line number
 * Return: nothing
 */
void print_top(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
